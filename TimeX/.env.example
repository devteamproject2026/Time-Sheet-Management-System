# Copy these values into the environment used to run the frontend.
# Change the addresses when the backend services are deployed elsewhere.
VITE_AUTH_API_URL=http://localhost:8081/api/auth
VITE_BUSINESS_API_URL=http://localhost:8082/api/business
